---
title: 啊实打实的
---

export const Hello = (props) => {
  return <div>Hello {props.children}</div>;
};

# <Hello>{Astro.props.frontmatter.title}</Hello>

```jsx
export const Hello = (props) => {
  return <div>Hello {props.children}</div>;
};
```